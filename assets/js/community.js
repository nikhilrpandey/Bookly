const showMore = document.getElementsByClassName('hide-content');

showMore.forEach(element => {
    element.on('click',()=>{
        if(element.switchClass)
    })    
});
   


// TODO: create a show more button to view the complete blog